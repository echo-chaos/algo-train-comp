package org.lql.practice.week07;

/**
 * @author: lql
 * @date: 2022/2/20 10:37 PM
 * @description: 1000. 合并石头的最低成本 https://leetcode-cn.com/problems/minimum-cost-to-merge-stones/
 */
public class MergeStones {

    /**
     * 有 N 堆石头排成一排，第 i 堆中有stones[i]块石头。
     * <p>
     * 每次移动（move）需要将连续的K堆石头合并为一堆，而这个移动的成本为这K堆石头的总数。
     * <p>
     * 找出把所有石头合并成一堆的最低成本。如果不可能，返回 -1 。
     * <p>
     * 1 <= stones.length <= 30
     * 2 <= K <= 30
     * 1 <= stones[i] <= 100
     */
    public int mergeStones(int[] stones, int k) {
        return 0;
    }
}
